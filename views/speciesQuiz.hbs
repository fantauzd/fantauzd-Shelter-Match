<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dog Breed Recommendation</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        /* Page background */
        body {
            color: #333;
            font-family: Arial, sans-serif;
        }

        /* Centering the form */
        .form-container {
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        h1 {
            margin-bottom: 35px;
            color: #333;
            text-align: center;
        }

        /* Form styling */
        form {
            background-color: #ffffff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            width: 535px;
        }

        label {
            font-size: 16px;
            color: #555;
            margin-top: 45px;
            display: block;
        }

        /* Slider styling */
        input[type="range"] {
            width: 100%;
            height: 8px;
            border-radius: 5px;
            background: linear-gradient(to right, #66bb6a, #ff7043);
            outline: none;
            -webkit-appearance: none;
            appearance: none;
            margin: 10px 0 20px;
        }

        input[type="range"]::-webkit-slider-thumb {
            -webkit-appearance: none;
            appearance: none;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            background-color: #66bb6a;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        input[type="range"]::-webkit-slider-thumb:hover {
            background-color: #ff7043;
        }

        button {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            background-color: #66bb6a;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        button:hover {
            background-color: #ff7043;
        }
    </style>
</head>
<body>
    <!-- Form Container to center the form without affecting the entire body -->
    <div class="form-container">
        <form action="/recommend-species" method="POST">
            <h1>Find Your Ideal Pet</h1>

            <label for="size">Preferred Size (1 = Small, 10 = Large):</label>
            <input type="range" name="size" id="size" min="1" max="10" required>

            <label for="livingSpace">Living Space (1 = Apartment, 10 = Large House):</label>
            <input type="range" name="livingSpace" id="livingSpace" min="1" max="10" required>

            <label for="interactionLevel">Interaction Level (1 = Low, 10 = High):</label>
            <input type="range" name="interactionLevel" id="interactionLevel" min="1" max="10" required>

            <label for="cost">Cost (1 = Affordable, 10 = Expensive):</label>
            <input type="range" name="cost" id="cost" min="1" max="10" required>

            <button type="submit">Get Recommendation</button>
        </form>
    </div>
</body>
</html>
